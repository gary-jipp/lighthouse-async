// Variables

// Variables hold values
const x = 5;
const name = "alice";

// Objects and Arrays can also be treated as values
// They can assigned to variables
const array = [1, 2, 3];

const object = {
  name: "Alice",
  age: 28
};

// You can treat the variable as if it wes the value itself
console.log(5);
console.log(x);

console.log([1, 2, 3]);
console.log(array);

